{
  "name": "pi-ssh-link",
  "version": "1.0.0",
  "description": "Persistent SSH bridge for AI remote system management — connect, execute, read/write files, auto-detect OS",
  "type": "module",
  "keywords": ["pi-package", "ssh", "remote", "server-management", "pi-extension"],
  "license": "MIT",
  "pi": {
    "extensions": ["./index.ts"],
    "skills": ["./skills"]
  },
  "dependencies": {
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "@types/ssh2": "^1.15.5"
  }
}
